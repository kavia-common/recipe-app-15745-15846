# Environment variables for Web Frontend
# Copy this file to .env and set values as needed.

# Base URL for the Backend API (must include /api suffix if backend expects it)
REACT_APP_API_BASE_URL=http://localhost:8080/api

# Optional: OAuth redirect URI (if social login is used)
REACT_APP_OAUTH_REDIRECT_URI=http://localhost:3000
